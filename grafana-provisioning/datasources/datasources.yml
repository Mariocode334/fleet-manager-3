apiVersion: 1

datasources:
  - name: MySQL
    type: mysql
    access: proxy
    url: mysql:3306
    database: fleet_db
    user: fleet_user
    password: fleet_password123
    isDefault: true
    editable: true
    uid: mysql-fleet
